@using AlphaCinema.Core.Constants
@using AlphaCinema.Core.ViewModels
@model IList<AdminTicketVM>

@{
    ViewData[ViewConstant.Title] = "Tickets";
}

<div class="card">
    <div class="card-header">
        <h3 class="card-title">Movie - @Model[0].MovieName</h3>
    </div>
    <div class="card-body p-0">
        <table class="table">
            <thead>
                <tr>
                    <th style="width: 10px">#</th>
                    <th style="width: 120px">Hall</th>
                    <th style="width: 120px">Row</th>
                    <th style="width: 120px">Column</th>
                    <th style="width: 270px">Start</th>
                    <th>Voucher</th>
                    <th>Price</th>
                    <th style="width: 40px">Purchased</th>
                </tr>
            </thead>
            <tbody>
                @for (int i = 0; i < Model.Count; i++)
                {
                    <tr>
                        <td>@(i+1)</td>
                        <td>@Model[i].HallNumber</td>
                        <td>@Model[i].Row</td>
                        <td>@Model[i].Column</td>
                        <td>@Model[i].Start</td>
                        <td>@Model[i].VoucherCode</td>
                        <td>@Model[i].Price lv.</td>
                        <td>@Model[i].IsPurchased</td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
    <div class="card-footer clearfix">
        <a asp-area="Administrator" asp-controller="Ticket" asp-action="Add" asp-route-id="@Model[0].MovieId" class="btn btn-primary float-right" style="max-width: 130px">Add Ticket</a>
    </div>
</div>